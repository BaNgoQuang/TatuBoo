
import { response } from "../utils/lib.js"

const fncStatisticNewRegisteredUsers = async (req) => {
  try {
    // const 
  } catch (error) {
    return response({}, true, error.toString(), 500)
  }
}

const StatisticService = {

}

export default StatisticService
